---
- name: Resolve Incident in ServiceNow
  hosts: localhost
  tasks:
    - name: Use snow_record to resolve an incident
      snow_record:
        instance: "https://dev141954.service-now.com"
        username: admin
        password: N$g6QjIQ2u%o
        table: incident
        state: present
        number: INC0000046
        data:
          state: 7  # Resolved state (adjust the state value according to your ServiceNow instance)
          short_description: "Solved"  # Adjust the close code as needed
          work_notes: "Incident has been resolved."  # Your resolution notes
      delegate_to: localhost
